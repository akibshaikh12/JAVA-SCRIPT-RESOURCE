// #primitiv data types 

// 7 typed : string, number , boolean, null, undefines, symbol, bigInt

const score = 100
const scoreValue = 100.3

const isLoggedIn =false
const outsideTemp = null
let userEmail;

const id = symbol('1234 ')

// 1 non-typed : object

// Reference (Non primitive)

// Arrays, Object, Functions